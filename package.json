{
  "private": true,
  "name": "nanojet-monorepo",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:engine": "npm run build --workspace=nanojet",
    "build:pong": "npm run build --workspace=pong",
    "dev": "npm run dev --workspace=pong"
  },
  "devDependencies": {
    "@types/node": "^22.7.6",
    "@webgpu/types": "^0.1.49",
    "typescript": "^5.6.3",
    "vite": "^5.4.9",
    "vite-plugin-dts": "^4.2.4"
  },
  "dependencies": {
    "zzfx": "^1.3.0"
  }
}
