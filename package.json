{
  "private": true,
  "name": "nanojet-monorepo",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:engine": "npm run build --workspace=engine",
    "build:game": "npm run build --workspace=game",
    "start:game": "npm run start --workspace=game"
  },
  "devDependencies": {
    "@types/node": "^22.7.6",
    "@webgpu/types": "^0.1.49",
    "typescript": "^5.6.3",
    "vite": "^5.4.9",
    "vite-plugin-dts": "^4.2.4"
  }
}
